/**
 * Publication metadata for the authored DSH Chrome Connector artifact.
 * @module @deepseek-ai/dsh-chrome-extension
 */

/** Package-relative directory containing the committed browser extension artifact. */
export const CHROME_EXTENSION_ARTIFACT_DIRECTORY = 'dist/browser-extension' as const

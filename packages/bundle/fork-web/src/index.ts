/**
 * @deepseek-ai/dsh-fork-web — fork-owned Web Host and client rows over
 * dsh-web-app. The package's substance is `cordis.patch.yml`, declared by the
 * `dsh.bundle.patch` manifest field and resolved by the profile composer
 * through that field; this module carries no runtime API.
 * @module @deepseek-ai/dsh-fork-web
 */

export {}
